spring:
  application:
    name: spring-boot-aws-deployment

#mysql configuration
  datasource:
    url: jdbc:mysql://localhost:3306/db_meal
    username:
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
